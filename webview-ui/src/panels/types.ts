export type TSubscription = {
    name: string;
    id: string;
};

export type TResourceGroup = {
    name: string;
    id: string;
};

export type TEventHub = {
    name: string;
    id: string;
};

export type TMesssage = {
    message: string;
};
